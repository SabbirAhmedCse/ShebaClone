class RequestService {
  final BigInt id;
  final String category;
  final String subCategory;
  final String serviceDate;
  final String serviceStatus;
  final String address;
  final String area;
  RequestService(this.id, this.category, this.subCategory, this.serviceDate,
      this.serviceStatus, this.address, this.area);
}
